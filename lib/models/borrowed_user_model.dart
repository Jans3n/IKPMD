class BorrowedUser {
  final String name;
  final String description;
  final int amount;

  const BorrowedUser({
    required this.name,
    required this.description,
    required this.amount,
  });
}